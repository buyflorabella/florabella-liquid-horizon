{% comment %}
  BFBella — Hero. Fully custom section (DXB, 2026-09-04: "look at Hydrogen.
  create a custom section, 100% custom CSS for the hero" — supersedes the
  earlier copy-modify-native-image-banner approach for this section only).
  Structure and CSS ported from the live Hydrogen reference
  (buyflorabella_restore/dev/frontend/app/componentsMockup2/components/
  HeroSection.tsx + index.css).

  Colors (DXB, 2026-09-04: "use the standard shopify color selectors, no
  custom CSS for colors" / "zero custom colors") reference the theme's one
  global settings.color_palette directly -- Horizon has no per-section
  color_scheme concept the way Dawn does (confirmed: no color_scheme_group
  anywhere in config/settings_schema.json, only the single global
  color_palette), so this section's colors follow the rest of the theme's
  dark palette rather than the light cream Dawn's hero uses -- Dawn can
  give one section its own standard-but-distinct scheme, Horizon's model
  can't without introducing a genuinely custom, non-standard color.
{% endcomment %}

<section class="bfbella-hero" id="BfbellaHero-{{ section.id }}">
  <div class="bfbella-hero__blobs" aria-hidden="true">
    <span class="bfbella-hero__blob bfbella-hero__blob--1"></span>
    <span class="bfbella-hero__blob bfbella-hero__blob--2"></span>
  </div>

  <div class="bfbella-hero__inner">
    <div class="bfbella-hero__content">
      {%- if section.settings.show_buy_now_pill -%}
        <div class="bfbella-hero__eyebrow">
          <a href="{{ section.settings.buy_now_link | default: '#' }}" class="bfbella-hero__pill bfbella-hero__pill--shiny">
            {{ section.settings.buy_now_label | default: 'Buy Now' }}
          </a>
        </div>
      {%- endif -%}

      <h1 class="bfbella-hero__heading">{{ section.settings.heading }}</h1>

      {%- if section.settings.subheading != blank -%}
        <p class="bfbella-hero__subheading">{{ section.settings.subheading }}</p>
      {%- endif -%}

      {%- if section.settings.body_text != blank -%}
        <div class="bfbella-hero__body rte">{{ section.settings.body_text }}</div>
      {%- endif -%}

      {%- if section.settings.callout_text != blank -%}
        <div class="bfbella-hero__callout rte">{{ section.settings.callout_text }}</div>
      {%- endif -%}

      {%- if section.settings.button_label_1 != blank or section.settings.button_label_2 != blank -%}
        <div class="bfbella-hero__buttons">
          {%- if section.settings.button_label_1 != blank -%}
            <a href="{{ section.settings.button_link_1 | default: '#' }}" class="bfbella-hero__button bfbella-hero__button--primary bfbella-hero__pill--shiny">
              {{ section.settings.button_label_1 }}
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
            </a>
          {%- endif -%}
          {%- if section.settings.button_label_2 != blank -%}
            <a href="{{ section.settings.button_link_2 | default: '#' }}" class="bfbella-hero__button bfbella-hero__button--secondary">
              {{ section.settings.button_label_2 }}
            </a>
          {%- endif -%}
        </div>
      {%- endif -%}

      {%- assign checkmarks = section.settings.checkmarks | newline_to_br | split: '<br />' -%}
      {%- if section.settings.checkmarks != blank -%}
        <ul class="bfbella-hero__checkmarks">
          {%- for item in checkmarks -%}
            {%- assign item = item | strip -%}
            {%- if item != blank -%}
              <li>
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M20 6 9 17l-5-5"/></svg>
                <span>{{ item }}</span>
              </li>
            {%- endif -%}
          {%- endfor -%}
        </ul>
      {%- endif -%}
    </div>

    <div class="bfbella-hero__media">
      <div class="bfbella-hero__media-glow" aria-hidden="true"></div>
      {%- if section.settings.image != blank -%}
        {{
          section.settings.image
          | image_url: width: 1600
          | image_tag:
            class: 'bfbella-hero__image',
            loading: 'eager',
            fetchpriority: 'high',
            widths: '400, 600, 800, 1200, 1600',
            sizes: '(min-width: 990px) 45vw, 90vw'
        }}
      {%- else -%}
        <div class="bfbella-hero__image-placeholder">{{ 'hero-apparel-1' | placeholder_svg_tag: 'bfbella-hero__image' }}</div>
      {%- endif -%}
    </div>
  </div>
</section>

<style>
  #BfbellaHero-{{ section.id }} {
    position: relative;
    overflow: hidden;
    background: {{ settings.color_palette.background }};
    padding: 4rem 0;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__blobs {
    position: absolute;
    inset: 0;
    overflow: hidden;
    opacity: 0.12;
    pointer-events: none;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__blob {
    position: absolute;
    border-radius: 50%;
    filter: blur(70px);
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__blob--1 {
    top: 5%;
    left: 5%;
    width: 26rem;
    height: 26rem;
    background: {{ settings.color_palette.color1 }};
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__blob--2 {
    bottom: 5%;
    right: 5%;
    width: 32rem;
    height: 32rem;
    background: {{ settings.color_palette.color2 }};
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__inner {
    position: relative;
    z-index: 1;
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 2rem;
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: center;
  }

  @media screen and (min-width: 990px) {
    #BfbellaHero-{{ section.id }} .bfbella-hero__inner {
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
    }
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__content {
    grid-column: 1;
    grid-row: 1;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__eyebrow {
    display: inline-block;
    background: {{ settings.color_palette.color1 }}1a;
    border: 1px solid {{ settings.color_palette.color1 }}4d;
    padding: 0.5rem;
    border-radius: 999px;
    margin-bottom: 1.5rem;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__pill {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.9rem 2rem;
    border-radius: 999px;
    background: {{ settings.color_palette.color1 }};
    color: {{ settings.color_palette.background }};
    font-weight: 700;
    font-size: 1.4rem;
    text-decoration: none;
    box-shadow: 0 10px 25px -5px {{ settings.color_palette.color1 }}66;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__pill:hover {
    transform: scale(1.08);
    box-shadow: 0 20px 35px -5px {{ settings.color_palette.color1 }}80;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__heading {
    font-family: var(--font-heading--family);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 0.95;
    font-size: 4.4rem;
    color: {{ settings.color_palette.foreground }};
    margin: 0 0 1.5rem;
  }

  @media screen and (min-width: 750px) {
    #BfbellaHero-{{ section.id }} .bfbella-hero__heading {
      font-size: 6rem;
    }
  }

  @media screen and (min-width: 990px) {
    #BfbellaHero-{{ section.id }} .bfbella-hero__heading {
      font-size: 7rem;
    }
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__subheading {
    font-size: 2rem;
    font-weight: 500;
    line-height: 1.4;
    color: {{ settings.color_palette.foreground }}d9;
    margin: 0 0 1.5rem;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__body {
    font-size: 1.6rem;
    line-height: 1.6;
    color: {{ settings.color_palette.foreground }}d9;
    max-width: 55rem;
    margin: 0 0 1.5rem;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__callout {
    background: {{ settings.color_palette.color1 }}0d;
    border: 1px solid {{ settings.color_palette.color1 }}33;
    border-radius: 1.5rem;
    padding: 1.5rem;
    margin: 0 0 2.5rem;
    max-width: 55rem;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__callout p {
    font-size: 1.3rem;
    line-height: 1.6;
    color: {{ settings.color_palette.foreground }}d9;
    margin: 0;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 2.5rem;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__button {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 1.1rem 2.4rem;
    border-radius: 999px;
    font-weight: 700;
    font-size: 1.5rem;
    text-decoration: none;
    transition: transform 0.3s ease, box-shadow 0.3s ease, background 0.3s ease;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__button--primary {
    background: {{ settings.color_palette.color1 }};
    color: {{ settings.color_palette.background }};
    box-shadow: 0 10px 25px -5px {{ settings.color_palette.color1 }}66;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__button--primary:hover {
    transform: scale(1.08);
    box-shadow: 0 20px 35px -5px {{ settings.color_palette.color1 }}80;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__button--secondary {
    background: transparent;
    border: 1px solid {{ settings.color_palette.foreground }}4d;
    color: {{ settings.color_palette.foreground }};
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__button--secondary:hover {
    transform: scale(1.05);
    border-color: {{ settings.color_palette.color1 }};
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__checkmarks {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__checkmarks li {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    font-size: 1.4rem;
    color: {{ settings.color_palette.foreground }}d9;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__checkmarks svg {
    color: {{ settings.color_palette.color1 }};
    flex-shrink: 0;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__media {
    position: relative;
    grid-column: 1;
    grid-row: 2;
  }

  @media screen and (min-width: 990px) {
    #BfbellaHero-{{ section.id }} .bfbella-hero__media {
      grid-column: 2;
      grid-row: 1;
      animation: bfbellaHeroParallaxFloat 5s ease-in-out infinite;
    }
  }

  @keyframes bfbellaHeroParallaxFloat {
    0%, 100% {
      transform: translateY(0) scale(1);
    }
    50% {
      transform: translateY(-1.5rem) scale(1.05);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    #BfbellaHero-{{ section.id }} .bfbella-hero__media {
      animation: none;
    }
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__media-glow {
    position: absolute;
    inset: -1.5rem;
    background: {{ settings.color_palette.color1 }}33;
    border-radius: 2.5rem;
    filter: blur(30px);
    z-index: 0;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__image,
  #BfbellaHero-{{ section.id }} .bfbella-hero__image-placeholder {
    position: relative;
    z-index: 1;
    width: 100%;
    height: auto;
    border-radius: 1.5rem;
    box-shadow: 0 25px 50px -12px {{ settings.color_palette.background }}40;
    display: block;
    transition: transform 0.7s ease;
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__media:hover .bfbella-hero__image {
    transform: scale(1.05);
  }

  #BfbellaHero-{{ section.id }} .bfbella-hero__image-placeholder svg {
    width: 100%;
    height: auto;
  }
</style>

{% schema %}
{
  "name": "BFBella — Hero",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "checkbox",
      "id": "show_buy_now_pill",
      "default": true,
      "label": "Show \"Buy Now\" eyebrow pill"
    },
    {
      "type": "text",
      "id": "buy_now_label",
      "default": "Buy now",
      "label": "Buy Now pill label"
    },
    {
      "type": "url",
      "id": "buy_now_link",
      "label": "Buy Now pill link"
    },
    {
      "type": "text",
      "id": "heading",
      "default": "Add a heading",
      "label": "Heading"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading"
    },
    {
      "type": "richtext",
      "id": "body_text",
      "label": "Body text"
    },
    {
      "type": "richtext",
      "id": "callout_text",
      "label": "Trust callout text"
    },
    {
      "type": "textarea",
      "id": "checkmarks",
      "label": "Checkmark list (one per line)"
    },
    {
      "type": "header",
      "content": "Buttons"
    },
    {
      "type": "text",
      "id": "button_label_1",
      "default": "Shop now",
      "label": "Primary button label"
    },
    {
      "type": "url",
      "id": "button_link_1",
      "label": "Primary button link"
    },
    {
      "type": "text",
      "id": "button_label_2",
      "label": "Secondary button label"
    },
    {
      "type": "url",
      "id": "button_link_2",
      "label": "Secondary button link"
    },
    {
      "type": "header",
      "content": "Image"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Product photo"
    },
    {
      "type": "paragraph",
      "content": "Colors follow the theme's global color palette (Theme Settings → Colors) — Horizon has no per-section scheme picker, so there are no color settings here to keep this zero-custom."
    }
  ],
  "presets": [
    {
      "name": "BFBella — Hero"
    }
  ]
}
{% endschema %}
